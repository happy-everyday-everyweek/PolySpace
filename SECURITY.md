# PolySpace 安全指南

## 安全最佳实践

### 1. 身份验证与访问控制

- **默认启用认证**：所有关键 API 端点（文件、工具、聊天等）都需要身份验证
- **使用强密码**：设置账户密码时，请使用至少 8 位的强密码，包含字母、数字和符号
- **定期更换密钥**：API Token 应定期更换，避免长期使用同一密钥

### 2. 数据安全

- **不要在代码中硬编码密钥**：所有敏感信息（API 密钥、数据库凭证等）应通过环境变量或配置文件提供
- **敏感数据加密**：用户密码使用 PBKDF2-HMAC 算法进行哈希存储
- **审计日志**：系统记录所有关键操作，便于追踪和审计

### 3. 部署安全

- **生产环境禁用调试模式**：在 `config.py` 中设置 `DEBUG=False`
- **配置 CORS**：在生产环境中配置严格的 CORS 策略，只允许信任的域名访问
- **使用 HTTPS**：在生产部署中，务必使用 HTTPS 加密所有通信
- **限制文件上传**：只允许上传安全的文件类型，并限制文件大小（最大 50MB）

### 4. 工具与权限控制

- **限制 Shell 工具使用**：Shell 执行工具具有高权限，应谨慎使用
- **验证工具来源**：安装第三方工具和技能时，确保来源可信
- **定期审查权限**：定期检查用户权限和访问级别

## 安全问题报告

如果您发现任何安全漏洞或问题，请按照以下步骤报告：

1. 详细描述问题，包括复现步骤
2. 提供受影响的版本和环境信息
3. 如果可能，提供修复建议

## 配置说明

### 环境变量

以下是与安全相关的关键环境变量：

- `POLYSPACE_DEBUG`：是否启用调试模式（生产环境应设为 `False`）
- `POLYSPACE_CORS_ORIGINS`：允许的 CORS 来源域名（逗号分隔）
- `POLYSPACE_LLM_*`：LLM 相关配置（请妥善保管 API 密钥）

### 数据库

- 主数据库使用 SQLite，存储在数据目录下
- 审计日志存储在独立的审计数据库中
- 所有数据库文件应设置适当的文件权限

## 已知限制

- 当前版本的身份验证为基础 Token 验证，生产环境建议考虑升级为 OAuth 2.0 或 JWT
- 文件上传的安全检查主要依赖扩展名白名单，建议结合内容验证
